# Agent Notes

- Always use `uv` for Python commands and dependency work.
- Run Ruff formatting before finalizing code changes.
- Before pushing SQLite DB changes, run `sqlite3 path/to.db "vacuum;"`.
- Keep code minimal, human-like, modular, and DRY. Keep changes scoped.
- Read relevant docs before changing behavior, and update docs when finalizing changes.
